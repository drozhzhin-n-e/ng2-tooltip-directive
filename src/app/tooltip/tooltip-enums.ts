export enum ContentType {
  String = 'string',
  Html = 'html',
  Template = 'template',
}

export enum Theme {
  Light = 'light',
  Dark = 'dark',
}

export enum Placement {
  Top = 'top',
  Right = 'right',
  Bottom = 'bottom',
  Left = 'left',
}
